######CUSTOM ALIASES BELOW######

# View running processes
alias p='ps -aef'

# Filter output
alias g='grep'

# Show disk usage in human readable format
alias df='df -H'

# Make some of the file manipulation programs verbose
alias mv="mv -v"
alias rm="rm -vi"
alias cp="cp -v"

# Easier way to go back directories
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias ......="cd ../../../../.."

######END CUSTOM ALIASES######
